#include <iostream>
#include <stdio.h>

    int num;
    printf("Enter an integer: ");
    scanf("%d", &num);
    int num;
    printf("Enter an integer: ");
    scanf("%d", &num);
    if (num % 2 == 0)
        printf("%d is even.", num);
    else
        printf("%d is odd.", num);
    return 0;
}
printf("hello world");